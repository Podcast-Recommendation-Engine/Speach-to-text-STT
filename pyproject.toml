[project]
name = "speach-to-text-stt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "confluent-kafka>=2.12.2",
    "dotenv>=0.9.9",
    "faster-whisper>=1.2.1",
]
